/** Subset dari dashboard/lib/work-mode.ts — hanya yang dibutuhkan form absen mobile. */

/** Penjelasan absen luar radius yang terlalu pendek tidak bisa dinilai admin — samakan dengan server. */
export const MIN_DETAIL_LENGTH = 5;
